" Tagbar configuration:

" Toggle the tagbar.
nmap <leader>tbt :TagbarToggle<CR>

" Open the tagbar and move the cursor over to it.
nmap <leader>tbj :TagbarOpen fj<CR>

" Open the tagbar, and auto-close it when you select a symbol.
nmap <leader>toac :TagbarOpenAutoClose<CR>
