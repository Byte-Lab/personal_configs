#
# The main .muttrc file.
#
# Taken in part from
# https://gitlab.com/muttmua/mutt/-/blob/master/contrib/sample.muttrc-starter.
#

# Default settings:
source "$PERSONAL_MUTT_DIR/defaults.rc"

# Mailbox (including identity) config setup:
source "$PERSONAL_MUTT_DIR/mailbox.rc"

# Sidebar config:
source "$PERSONAL_MUTT_DIR/sidebar.rc"

# Manual bindings:
source "$PERSONAL_MUTT_DIR/keybindings.rc"

# Colors:
source "$PERSONAL_MUTT_DIR/colors.rc"
